using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // day la cong viec cua nhom nha
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
